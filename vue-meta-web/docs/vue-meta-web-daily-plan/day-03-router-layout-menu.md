# Day 03 - 路由、布局、菜单

## 今日目标

迁移完整路由结构，做出主框架。

## Vue 差异点

- Vue Router 用 route records，不再依赖 Umi routes。
- 菜单高亮通过 `useRoute()` 和 path 匹配实现。

## Todo

- 建立路由表，覆盖工作台、迭代、需求、集成发布、发布记录、告警、配置中心。
- 实现登录页、主布局、详情页路由。
- 实现侧边菜单，过滤 `isMenu: false` 的详情页。
- 实现顶部用户区占位和退出按钮占位。
- 实现移动端侧边栏折叠。

## 验收

- 每个菜单能跳转到对应占位页。
- 详情页不出现在菜单中。
- 刷新页面后菜单高亮正确。
